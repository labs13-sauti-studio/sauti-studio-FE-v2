import React from 'react';

// import FolderImage from '../images/FolderBlank.png';

const Folder = (props) => {
  return (
    <div className="folder">
      <h1>{ props.title }</h1>
    </div>
  )
}

export default Folder;
